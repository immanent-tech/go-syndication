// Package sitemap provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.5.0 DO NOT EDIT.
package sitemap

import (
	"encoding/xml"

	"github.com/immanent-tech/go-syndication/types"
)

// Defines values for AccessRestriction.
const (
	AccessRestrictionRegistration AccessRestriction = "Registration"
	AccessRestrictionSubscription AccessRestriction = "Subscription"
)

// Defines values for URLChangefreq.
const (
	URLChangefreqAlways  URLChangefreq = "always"
	URLChangefreqDaily   URLChangefreq = "daily"
	URLChangefreqHourly  URLChangefreq = "hourly"
	URLChangefreqMonthly URLChangefreq = "monthly"
	URLChangefreqNever   URLChangefreq = "never"
	URLChangefreqWeekly  URLChangefreq = "weekly"
	URLChangefreqYearly  URLChangefreq = "yearly"
)

// Access is the accessibility of the article. Required if access is not open, otherwise this tag should be omitted.
type Access struct {
	Restriction *AccessRestriction `json:"restriction,omitempty" validate:"required,oneof=Subscription Registration" xml:"http://www.google.com/schemas/sitemap-news/0.9 restriction"`
}

// AccessRestriction defines model for Access.Restriction.
type AccessRestriction string

// Image contains details about an image.
type Image struct {
	// Loc URL to the image.
	Loc types.String `json:"loc" validate:"required,url" xml:"http://www.google.com/schemas/sitemap-image/1.1 loc"`
}

// LOC URL of the page. This URL must begin with the protocol (such as http) and end with a trailing slash, if your web server requires it. This value must be less than 2,048 characters.
type LOC = types.String

// LastMod The date of last modification of the page. This date should be in W3C Datetime format. This format allows you to omit the time portion, if desired, and use YYYY-MM-DD. Note that the date must be set to the date the linked page was last modified, not when the sitemap is generated. Note also that this tag is separate from the If-Modified-Since (304) header the server can return, and search engines may use the information from both sources differently.
type LastMod = types.DateTime

// News indicates a URL is a news article and contains additional related elements.
type News struct {
	// Access is the accessibility of the article. Required if access is not open, otherwise this tag should be omitted.
	Access *Access `json:"access" validate:"omitempty" xml:"http://www.google.com/schemas/sitemap-news/0.9 access"`

	// Publication contains details about the publication for the news item.
	Publication *Publication `json:"publication" xml:"http://www.google.com/schemas/sitemap-news/0.9 publication"`

	// PublicationDate The article publication date in W3C format. Use either the "complete date" format (YYYY-MM-DD) or the "complete date plus hours, minutes, and seconds" format with time zone designator format (YYYY-MM-DDThh:mm:ssTZD). Specify the original date and time when the article was first published on your site. Don't specify the time when you added the article to your sitemap.
	PublicationDate *PublicationDate `json:"publication_date" validate:"omitempty" xml:"http://www.google.com/schemas/sitemap-news/0.9 publication_date"`

	// Title The title of the news article.
	Title *Title `json:"title" validate:"omitempty" xml:"http://www.google.com/schemas/sitemap-news/0.9 title"`
}

// Publication contains details about the publication for the news item.
type Publication struct {
	// Language is the language of your publication. Use an ISO 639 language code (two or three letters).
	Language *string `json:"language,omitempty" validate:"omitempty,iso3166_1_alpha2|iso3166_1_alpha3|bcp47_language_tag" xml:"http://www.google.com/schemas/sitemap-news/0.9 language"`

	// Name is the name of the news publication. It must exactly match the name as it appears on your articles on news.google.com, omitting anything in parentheses.
	Name *types.String `json:"name,omitempty" validate:"omitempty" xml:"http://www.google.com/schemas/sitemap-news/0.9 name"`
}

// PublicationDate The article publication date in W3C format. Use either the "complete date" format (YYYY-MM-DD) or the "complete date plus hours, minutes, and seconds" format with time zone designator format (YYYY-MM-DDThh:mm:ssTZD). Specify the original date and time when the article was first published on your site. Don't specify the time when you added the article to your sitemap.
type PublicationDate = types.DateTime

// Sitemap encapsulates information about an individual Sitemap.
type Sitemap struct {
	// Lastmod The date of last modification of the page. This date should be in W3C Datetime format. This format allows you to omit the time portion, if desired, and use YYYY-MM-DD. Note that the date must be set to the date the linked page was last modified, not when the sitemap is generated. Note also that this tag is separate from the If-Modified-Since (304) header the server can return, and search engines may use the information from both sources differently.
	Lastmod *LastMod `json:"lastmod,omitempty" validate:"omitempty" xml:"lastmod"`

	// Loc URL of the page. This URL must begin with the protocol (such as http) and end with a trailing slash, if your web server requires it. This value must be less than 2,048 characters.
	Loc LOC `json:"loc" validate:"required,url" xml:"loc"`
}

// SitemapIndex encapsulates information about all of the Sitemaps in the file.
type SitemapIndex struct {
	// Sitemaps is a list of sitemaps.
	Sitemaps []Sitemap `json:"sitemaps" xml:"sitemap"`

	// XMLName represents the XML namespace of an element.
	XMLName xml.Name `json:"XMLName"`
}

// Title The title of the news article.
type Title = types.String

// URL describes a URL in the sitemap
type URL struct {
	// Changefreq How frequently the page is likely to change. This value provides general information to search engines and may not correlate exactly to how often they crawl the page.
	Changefreq *URLChangefreq `json:"changefreq,omitempty" validate:"omitempty,oneof=always hourly daily weekly monthly yearly never" xml:"changefreq"`

	// Image contains details about an image.
	Image *Image `json:"image" xml:"http://www.google.com/schemas/sitemap-image/1.1 image"`

	// Lastmod The date of last modification of the page. This date should be in W3C Datetime format. This format allows you to omit the time portion, if desired, and use YYYY-MM-DD. Note that the date must be set to the date the linked page was last modified, not when the sitemap is generated. Note also that this tag is separate from the If-Modified-Since (304) header the server can return, and search engines may use the information from both sources differently.
	Lastmod *LastMod `json:"lastmod,omitempty" validate:"omitempty" xml:"lastmod"`

	// Loc URL of the page. This URL must begin with the protocol (such as http) and end with a trailing slash, if your web server requires it. This value must be less than 2,048 characters.
	Loc LOC `json:"loc" validate:"required,url" xml:"loc"`

	// News indicates a URL is a news article and contains additional related elements.
	News *News `json:"news,omitempty" xml:"http://www.google.com/schemas/sitemap-news/0.9 news"`

	// Priority The priority of this URL relative to other URLs on your site. Valid values range from 0.0 to 1.0. This value does not affect how your pages are compared to pages on other sites—it only lets the search engines know which pages you deem most important for the crawlers.
	Priority float32 `json:"priority,omitempty" validate:"gte=0.0,lte=1.0" xml:"priority"`
}

// URLChangefreq How frequently the page is likely to change. This value provides general information to search engines and may not correlate exactly to how often they crawl the page.
type URLChangefreq string

// URLSet Encapsulates the file and references the current protocol standard.
type URLSet struct {
	// URLs is the list of URLs contained within the sitemap.
	URLs []URL `json:"urls" xml:"url"`

	// XMLName represents the XML namespace of an element.
	XMLName xml.Name `json:"XMLName"`
}
